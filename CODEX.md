See [AGENTS.md](AGENTS.md) for project instructions. It links out to
`AGENTS/` for per-area detail (privacy, design tokens, backend, frontend,
git workflow, no-slop) — read the relevant one for whatever you're touching.
