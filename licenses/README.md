# licenses

license texts of the third-party libraries Pelton ships, read at runtime by the app.

- `go/` — go module licenses (filename = module path, `/` replaced by `_`)
- `npm/` — frontend package licenses

regenerate after adding a dependency by copying the lib's LICENSE file into the matching folder.
